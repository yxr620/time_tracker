# 阿里云 OSS 配置（多端同步功能）
# Aliyun OSS Configuration (for multi-device sync)

# OSS 区域（例如：oss-cn-hangzhou, oss-cn-beijing）
# OSS Region (e.g., oss-cn-hangzhou, oss-cn-beijing)
VITE_OSS_REGION=oss-cn-hangzhou

# OSS Bucket 名称
# OSS Bucket Name
VITE_OSS_BUCKET=your-bucket-name

# OSS Access Key ID
VITE_OSS_ACCESS_KEY_ID=your-access-key-id

# OSS Access Key Secret
VITE_OSS_ACCESS_KEY_SECRET=your-access-key-secret

# 说明：
# 1. 如果不配置 OSS，同步功能将不会显示，但应用其他功能正常使用
# 2. 可以在阿里云控制台获取 AccessKey：https://ram.console.aliyun.com/manage/ak
# 3. 需要在 OSS 控制台创建 Bucket 并配置跨域访问（CORS）
# 4. 建议使用 RAM 用户的 AccessKey，限制权限范围
#
# Notes:
# 1. If OSS is not configured, the sync feature won't be shown, but other features work normally
# 2. Get AccessKey from Aliyun Console: https://ram.console.aliyun.com/manage/ak
# 3. Create a Bucket in OSS Console and configure CORS
# 4. Recommend using RAM user's AccessKey with limited permissions
